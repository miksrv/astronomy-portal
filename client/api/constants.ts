import { HOST_IMG } from '@/api/api'

// TODO Refactoring this file
export const hosts = {
    fits: `${HOST_IMG}fits/`,
    maps: `${HOST_IMG}maps/`,
    photo: `${HOST_IMG}astrophotos/`,
    temp: `${HOST_IMG}temp/`
}
