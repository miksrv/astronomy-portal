import { imageHost } from '@/api/api'

export const hosts = {
    fits: `${imageHost}fits/`,
    maps: `${imageHost}maps/`,
    photo: `${imageHost}photos/`,
    post: `${imageHost}posts/`,
    temp: `${imageHost}temp/`
}
